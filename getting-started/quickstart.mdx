---
title: 'Quickstart'
description: 'Get Ticket Hub up and running in minutes'
icon: 'rocket'
---

## Overview

Ticket Hub is a full-stack ticket marketplace platform built with Next.js 15, Convex, Clerk, and Stripe. This guide will help you set up the project locally in just a few minutes.

<CardGroup cols={2}>
  <Card title="Next.js 15" icon="react" iconType="duotone">
    Built on the latest Next.js with App Router and React 19
  </Card>
  <Card title="Convex Backend" icon="database" iconType="duotone">
    Real-time database with reactive queries
  </Card>
  <Card title="Clerk Auth" icon="shield-check" iconType="duotone">
    Secure authentication and user management
  </Card>
  <Card title="Stripe Payments" icon="credit-card" iconType="duotone">
    Payment processing for ticket purchases
  </Card>
</CardGroup>

## Prerequisites

Before you begin, ensure you have:

- **Node.js** version 18.8 or higher
- A **Clerk** account ([clerk.com](https://clerk.com))
- A **Convex** account ([convex.dev](https://convex.dev))
- A **Stripe** account ([stripe.com](https://stripe.com))

## Installation

<Steps>
  <Step title="Clone the repository">
    Clone the Ticket Hub repository to your local machine:

    <CodeGroup>
    ```bash npm
    git clone https://github.com/your-username/ticket-hub.git
    cd ticket-hub
    ```

    ```bash yarn
    git clone https://github.com/your-username/ticket-hub.git
    cd ticket-hub
    ```

    ```bash pnpm
    git clone https://github.com/your-username/ticket-hub.git
    cd ticket-hub
    ```
    </CodeGroup>
  </Step>

  <Step title="Install dependencies">
    Install all required packages:

    <CodeGroup>
    ```bash npm
    npm install
    ```

    ```bash yarn
    yarn install
    ```

    ```bash pnpm
    pnpm install
    ```
    </CodeGroup>

    This will install key dependencies including:
    - `@clerk/nextjs` - Authentication
    - `convex` - Backend and database
    - `stripe` - Payment processing
    - `@tanstack/react-query` - Data fetching
  </Step>

  <Step title="Set up environment variables">
    Create a `.env.local` file in the root directory with the required environment variables:

    ```bash
    cp .env.example .env.local
    ```

    Fill in the values (see [Environment Setup](/getting-started/environment-setup) for detailed instructions):

    ```bash
    # Clerk Authentication
    NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
    CLERK_SECRET_KEY=sk_test_...
    CLERK_WEBHOOK_SECRET=whsec_...

    # Clerk URLs
    NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
    NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up

    # Convex
    NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud
    CONVEX_DEPLOYMENT=your-deployment-name
    CONVEX_DEPLOY_KEY=...

    # Stripe
    STRIPE_SECRET_KEY=sk_test_...
    STRIPE_WEBHOOK_SECRET=whsec_...

    # App Configuration
    NEXT_PUBLIC_APP_URL=http://localhost:3000
    VERCEL_PROJECT_PRODUCTION_URL=your-production-url.vercel.app
    ```
  </Step>

  <Step title="Initialize Convex">
    Set up your Convex backend:

    <CodeGroup>
    ```bash npm
    npx convex dev
    ```

    ```bash yarn
    yarn convex dev
    ```

    ```bash pnpm
    pnpm convex dev
    ```
    </CodeGroup>

    This will:
    - Create a new Convex project (if needed)
    - Deploy your schema and functions
    - Generate the `NEXT_PUBLIC_CONVEX_URL`
  </Step>

  <Step title="Start the development server">
    Run the development server:

    <CodeGroup>
    ```bash npm
    npm run dev
    ```

    ```bash yarn
    yarn dev
    ```

    ```bash pnpm
    pnpm dev
    ```
    </CodeGroup>

    Open [http://localhost:3000](http://localhost:3000) in your browser to see the application.
  </Step>
</Steps>

## Your First Event

Once the app is running, you can create your first event:

<Steps>
  <Step title="Sign up and authenticate">
    Navigate to `/sign-up` and create an account using Clerk authentication.
  </Step>

  <Step title="Navigate to seller dashboard">
    Go to `/seller/new-event` to access the event creation form.
  </Step>

  <Step title="Create an event">
    Fill in the event details:

    ```typescript
    // The form submits to the Convex mutation:
    // convex/events.ts:43-65
    const eventData = {
      name: "Summer Music Festival",
      description: "An amazing outdoor concert experience",
      location: "Central Park, New York",
      eventDate: new Date('2026-07-15').getTime(),
      price: 99.99,
      totalTickets: 500,
      userId: user.id // Automatically set from auth
    };
    ```
  </Step>

  <Step title="Event is live">
    Your event is now live! Users can:
    - Browse events on the homepage
    - Join waiting lists
    - Purchase tickets with Stripe
  </Step>
</Steps>

## Key Features

<CardGroup cols={2}>
  <Card title="Event Management" icon="calendar">
    Create, edit, and manage events with real-time ticket tracking
  </Card>
  <Card title="Waiting List System" icon="users">
    Automatic queue management with time-limited ticket offers
  </Card>
  <Card title="Secure Payments" icon="lock">
    PCI-compliant payment processing with Stripe
  </Card>
  <Card title="Real-time Updates" icon="bolt">
    Live ticket availability using Convex reactive queries
  </Card>
</CardGroup>

## Next Steps

<CardGroup cols={2}>
  <Card title="Authentication" icon="key" href="/getting-started/authentication">
    Learn how to configure Clerk authentication
  </Card>
  <Card title="Environment Setup" icon="gear" href="/getting-started/environment-setup">
    Detailed environment variable configuration
  </Card>
  <Card title="API Reference" icon="code" href="/api/events/overview">
    Explore the Convex API functions
  </Card>
  <Card title="Feature Guides" icon="book" href="/guides/creating-events">
    Learn how to use Ticket Hub features
  </Card>
</CardGroup>

## Troubleshooting

<AccordionGroup>
  <Accordion title="Convex connection errors">
    Make sure you've run `npx convex dev` and copied the `NEXT_PUBLIC_CONVEX_URL` to your `.env.local` file.
  </Accordion>

  <Accordion title="Clerk authentication not working">
    Verify that:
    - Your Clerk keys are correctly set in `.env.local`
    - The sign-in/sign-up URLs match your Clerk dashboard configuration
    - You've added `http://localhost:3000` to your Clerk allowed origins
  </Accordion>

  <Accordion title="Type errors after installation">
    Run `npx convex dev` to generate the Convex types, then restart your TypeScript server.
  </Accordion>

  <Accordion title="Clean install needed">
    If you encounter persistent issues, try a clean install:
    
    ```bash npm
    npm run clean
    npm install
    npm run dev
    ```
  </Accordion>
</AccordionGroup>
